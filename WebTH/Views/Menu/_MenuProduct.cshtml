@model IEnumerable<WebTH.Models.EF.ProductCategory>

@if (Model != null && Model.Any())
{
<div class="wrap-label-gallery filter-tope-group size27 flex-w flex-sb-m m-l-r-auto flex-col-c-sm p-l-15 p-r-15 m-b-60">
    <button class="label-gallery txt26 trans-0-4 is-actived" data-filter="*">
        Tất cả
    </button>
    @foreach (var item in Model)
    {
        <button class="label-gallery txt26 trans-0-4" data-filter=".@item.Alias">
            @item.Title
        </button>
    }
    @*<button class="label-gallery txt26 trans-0-4" data-filter=".milk">
        Sữa
    </button>
    <button class="label-gallery txt26 trans-0-4" data-filter=".yogurt">
        Sữa chua
    </button>
    <button class="label-gallery txt26 trans-0-4" data-filter=".juice">
        Nước trái cây
    </button>
    <button class="label-gallery txt26 trans-0-4" data-filter=".food">
        Đồ ăn
    </button>*@
</div>
    <div class="wrap-gallery isotope-grid flex-w p-l-25 p-r-25">
        @Html.Action("Partial_ItemByCateId", "ProductList")
    </div>
    @*<div class="pagination flex-c-m flex-w p-l-15 p-r-15 m-t-24 m-b-50">
        <a href="#" class="item-pagination flex-c-m trans-0-4 active-pagination">1</a>
        <a href="#" class="item-pagination flex-c-m trans-0-4">2</a>
        <a href="#" class="item-pagination flex-c-m trans-0-4">3</a>
    </div>*@
}